package org.example.jdbc;

public class JBDCConfigure {
/*    String url = "jdbc:mysql://localhost:3306/Sudeep";
    String query = "select * from Sudeep.Drivers_License where Driver_ID= '1234'";


        Class.forName("com.mysql.cj.jdbc.Driver");
    Connection connection = DriverManager.getConnection(url,"root","javauser");
    Statement statement = connection.createStatement();
    ResultSet resultSet= statement.executeQuery(query);
    ResultSetMetaData metaData = resultSet.getMetaData();
    int columnCount = metaData.getColumnCount();

        while (resultSet.next()) {
        // Iterate through each column and retrieve values
        for (int i = 1; i <= columnCount; i++) {
            String columnName = metaData.getColumnName(i);
            Object value = resultSet.getObject(i);
            System.out.println(columnName + ": " + value);
        }
        System.out.println(); // Print an empty line between rows
    }

//        System.out.println(name);
        statement.close();
        connection.close();*/

}
